"use client";

/**
 * Native smooth-scroll for in-page anchors.
 *
 * Uses the browser's native scroll-behavior + scroll-margin-top, which is
 * hardware-accelerated and doesn't get starved by heavy paint work the way
 * a JS-driven RAF scroll loop (Lenis) does.
 */
export default function SmoothScroll() {
  return null;
}
